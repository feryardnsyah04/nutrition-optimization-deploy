version: "3.9"

services:
  app:
    build: .
    ports:
      - "5173:5173"
      - "4000:4000"
      - "8000:8000"
    environment:
      FASTAPI_BASE_URL: "http://localhost:8000"
      GEMINI_API_KEY: "${GEMINI_API_KEY}"
    volumes:
      - .:/app
    command: npm run dev
